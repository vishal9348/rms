spring.application.name=rms

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:3307/rms_db
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Show SQL Queries
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Server Configuration
server.servlet.context-path=/api/v1/rms
server.port=8080

#use this for swagger ui
#http://localhost:8080/api/v1/rms/swagger-ui/index.html

# JWT Secret
jwt.secretKey="oiygvhbnciuwhobucwndciuwogvhwuinviuvncwuvbcwunv1232cd"

# Frontend URL
#frontend.url=${FRONTEND_URL}
frontend.url=http://localhost:3000

# Stripe Configuration
stripe.secret.key=${STRIPE_SECRET_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}

# Swagger Configuration
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
